# orionchill

Static Artemis II / Orion orbit animation assets and viewer.

## Run locally

```sh
./start.sh
```

Or serve the directory directly:

```sh
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
